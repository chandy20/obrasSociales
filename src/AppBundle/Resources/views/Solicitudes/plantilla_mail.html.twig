<div class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box box-primary">
                <div class="box-body">
                    <div class="col-xs-12">
                        <h3 class="text-center help-block">{{ 'email.saludo' | trans }}</h3>
                    </div>
                    <div class="col-xs-12">
                        <p>{{ 'email.saludo' | trans }}</p>
                        <br>
                        <p>{{'email.contenido'|trans}} {% if concepto.aprobado %} <b>Aprobada</b> {% else %} <b>Rechazada</b> {% endif %}</p>
                        <br>
                        <br>
                        <p>{{'email.segundo_contenido'|trans}}</p>
                        <br>
                        <p>{{'email.area'|trans}}</p>
                        <p>{{'email.asociacion'|trans}}</p>
                        <p>{{'email.email'|trans}}</p>
                        <p>{{'email.pbx'|trans}}</p>
                        <p>{{'email.atencion'|trans}}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
