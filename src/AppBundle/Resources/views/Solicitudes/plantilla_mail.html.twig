<div class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box box-primary">
                <div class="box-body">
                    <div class="col-xs-12">
                        <h3 class="text-center help-block">{{ 'email.saludo' | trans }}</h3>
                    </div>
                    <div class="col-xs-12">
                        <p>{{ 'email.saludo' | trans }}</p>
                        <br>
                        <p>{{'email.contenido'|trans}}</p>
                        <br>
                        <p>{{ 'email.saludo' | trans }}</p>
                        <br>
                        <p>{{'email.segundo_contenido'|trans}}</p>
                        <br>
                        <p>{{'email.portal'|trans}}</p>
                        <p>{{'email.twitter'|trans}}</p>
                        <p>{{'email.facebook'|trans}}</p>
                        <p>{{'email.instagram'|trans}}</p>
                        <p>{{'email.pbx'|trans}}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
